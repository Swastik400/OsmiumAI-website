{
  "buildCommand": "cd osmium-new && npm run build",
  "outputDirectory": "osmium-new/.next",
  "installCommand": "cd osmium-new && rm -f package-lock.json && npm install --legacy-peer-deps",
  "framework": "nextjs"
}
